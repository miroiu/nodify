<Window x:Class="Nodify.StateMachine.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Nodify.StateMachine"
        xmlns:nodify="http://miroiu.github.io/winfx/xaml/nodify"
        ResizeMode="CanResizeWithGrip"
        mc:Ignorable="d"
        Title="State Machine Editor"
        Height="450"
        Width="800">
    <Window.DataContext>
        <local:StateMachineViewModel />
    </Window.DataContext>

    <Window.Resources>
        <local:BindingProxy x:Key="EditorProxy"
                            DataContext="{Binding}" />
        <local:HalfSizeWithOffsetConverter x:Key="HalfSizeWithOffsetConverter" />
    </Window.Resources>

    <Grid>
        <nodify:NodifyEditor ItemsSource="{Binding States}"
                             SelectedItem="{Binding SelectedState}"
                             SelectedItems="{Binding SelectedStates}"
                             Connections="{Binding Transitions}"
                             PendingConnection="{Binding PendingTransition}"
                             DisconnectConnectorCommand="{Binding DisconnectStateCommand}"
                             ConnectionCompletedCommand="{Binding CreateTransitionCommand}">
            <nodify:NodifyEditor.Resources>
                <Style TargetType="{x:Type nodify:PendingConnection}"
                       BasedOn="{StaticResource {x:Type nodify:PendingConnection}}">
                    <Setter Property="StrokeDashArray"
                            Value="" />
                    <Setter Property="EnablePreview"
                            Value="True" />
                    <Setter Property="BorderBrush"
                            Value="White" />
                </Style>
            </nodify:NodifyEditor.Resources>

            <nodify:NodifyEditor.PendingConnectionTemplate>
                <DataTemplate DataType="{x:Type local:TransitionViewModel}">
                    <nodify:PendingConnection Source="{Binding Source, Mode=OneWayToSource}"
                                              Target="{Binding Target, Mode=OneWayToSource}">
                        <nodify:PendingConnection.Template>
                            <ControlTemplate TargetType="{x:Type nodify:PendingConnection}">
                                <nodify:DirectionalConnection Source="{TemplateBinding SourceAnchor}"
                                                              Target="{TemplateBinding TargetAnchor}"
                                                              Stroke="{TemplateBinding BorderBrush}"
                                                              StrokeThickness="{TemplateBinding StrokeThickness}"
                                                              StrokeDashArray="{TemplateBinding StrokeDashArray}"
                                                              Fill="{TemplateBinding BorderBrush}"
                                                              SourceOffset="{Binding Source.Size}"
                                                              OffsetMode="Edge" />
                            </ControlTemplate>
                        </nodify:PendingConnection.Template>
                    </nodify:PendingConnection>
                </DataTemplate>
            </nodify:NodifyEditor.PendingConnectionTemplate>

            <nodify:NodifyEditor.ConnectionTemplate>
                <DataTemplate DataType="{x:Type local:TransitionViewModel}">
                    <nodify:DirectionalConnection Source="{Binding Source.Anchor}"
                                                  Target="{Binding Target.Anchor}"
                                                  SourceOffset="{Binding Source.Size}"
                                                  TargetOffset="{Binding Target.Size}"
                                                  OffsetMode="Edge"
                                                  Tag="{Binding}">
                        <nodify:DirectionalConnection.Style>
                            <Style TargetType="{x:Type nodify:DirectionalConnection}">
                                <Setter Property="StrokeThickness"
                                        Value="3" />
                                <Setter Property="Stroke"
                                        Value="White" />
                                <Setter Property="Fill"
                                        Value="White" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsActive}"
                                                 Value="True">
                                        <Setter Property="Stroke"
                                                Value="#8DD28A" />
                                        <Setter Property="StrokeThickness"
                                                Value="6" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </nodify:DirectionalConnection.Style>
                        <nodify:DirectionalConnection.ContextMenu>
                            <ContextMenu DataContext="{Binding DataContext, Source={StaticResource EditorProxy}}">
                                <MenuItem Header="_Delete"
                                          Icon="{StaticResource DeleteIcon}"
                                          Command="{Binding DeleteTransitionCommand}"
                                          CommandParameter="{Binding PlacementTarget.Tag, RelativeSource={RelativeSource AncestorType={x:Type ContextMenu}}}" />
                            </ContextMenu>
                        </nodify:DirectionalConnection.ContextMenu>
                    </nodify:DirectionalConnection>
                </DataTemplate>
            </nodify:NodifyEditor.ConnectionTemplate>

            <nodify:NodifyEditor.ItemTemplate>
                <DataTemplate DataType="{x:Type local:StateViewModel}">
                    <!--If IsConnected is false, Anchor won't be updated-->
                    <nodify:StateNode Content="{Binding}"
                                      IsConnected="True"
                                      Size="{Binding Size, Mode=OneWayToSource, Converter={StaticResource HalfSizeWithOffsetConverter}, ConverterParameter=5}"
                                      Anchor="{Binding Anchor, Mode=OneWayToSource}">
                        <nodify:StateNode.ContentTemplate>
                            <DataTemplate DataType="{x:Type local:StateViewModel}">
                                <local:EditableTextBlock Text="{Binding Name}"
                                                         IsEditing="{Binding IsRenaming}"
                                                         IsEditable="{Binding IsEditable}"
                                                         MaxLength="30" />
                            </DataTemplate>
                        </nodify:StateNode.ContentTemplate>
                        <nodify:StateNode.Style>
                            <Style TargetType="{x:Type nodify:StateNode}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsEditable}"
                                                 Value="False">
                                        <Setter Property="BorderBrush"
                                                Value="#E6AF86" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsActive}"
                                                 Value="True">
                                        <Setter Property="BorderBrush"
                                                Value="#8DD28A" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </nodify:StateNode.Style>
                    </nodify:StateNode>
                </DataTemplate>
            </nodify:NodifyEditor.ItemTemplate>

            <nodify:NodifyEditor.ItemContainerStyle>
                <Style TargetType="{x:Type nodify:ItemContainer}"
                       BasedOn="{StaticResource {x:Type nodify:ItemContainer}}">
                    <Setter Property="BorderBrush"
                            Value="Transparent" />
                    <Setter Property="Location"
                            Value="{Binding Location}" />
                    <Setter Property="ContextMenu">
                        <Setter.Value>
                            <ContextMenu DataContext="{Binding DataContext, Source={StaticResource EditorProxy}}">
                                <MenuItem Header="_Delete"
                                          Icon="{StaticResource DeleteIcon}"
                                          Command="{Binding DeleteSelectionCommand}" />
                                <MenuItem Header="Di_sconnect"
                                          Icon="{StaticResource DisconnectIcon}"
                                          Command="{Binding DisconnectSelectionCommand}" />
                                <MenuItem Header="_Rename"
                                          Icon="{StaticResource RenameIcon}"
                                          Command="{Binding RenameStateCommand}" />
                            </ContextMenu>
                        </Setter.Value>
                    </Setter>
                </Style>
            </nodify:NodifyEditor.ItemContainerStyle>

            <nodify:NodifyEditor.ContextMenu>
                <ContextMenu DataContext="{Binding DataContext, Source={StaticResource EditorProxy}}">
                    <MenuItem Header="_Add State"
                              Icon="{StaticResource AddStateIcon}"
                              InputGestureText="Shift+A"
                              Command="{Binding AddStateCommand}"
                              CommandParameter="{Binding PlacementTarget.MouseLocation, RelativeSource={RelativeSource AncestorType={x:Type ContextMenu}}}" />
                    <MenuItem Header="_Delete"
                              Icon="{StaticResource DeleteIcon}"
                              InputGestureText="Delete"
                              Command="{Binding DeleteSelectionCommand}" />
                    <Separator Background="#2D2D30" />
                    <MenuItem Header="_Select All"
                              Icon="{StaticResource SelectAllIcon}"
                              InputGestureText="Ctrl+A"
                              Command="{Binding SelectAllCommand}" />
                </ContextMenu>
            </nodify:NodifyEditor.ContextMenu>

            <nodify:NodifyEditor.InputBindings>
                <KeyBinding Key="Delete"
                            Command="{Binding DeleteSelectionCommand}" />
                <KeyBinding Key="A"
                            Modifiers="Shift"
                            Command="{Binding AddStateCommand}"
                            CommandParameter="{Binding MouseLocation, RelativeSource={RelativeSource AncestorType={x:Type nodify:NodifyEditor}}}" />
                <KeyBinding Gesture="Ctrl+A"
                            Command="{Binding SelectAllCommand}" />
            </nodify:NodifyEditor.InputBindings>
        </nodify:NodifyEditor>

        <!--TRANSITIONS-->
        <Expander Header="Transitions"
                  HorizontalAlignment="Left"
                  Background="#2D2D30"
                  Padding="4 1 0 3"
                  ExpandDirection="Right"
                  IsExpanded="True">
            <Expander.Resources>
                <DataTemplate x:Key="BooleanTemplate"
                              DataType="{x:Type local:BlackboardKeyViewModel}">
                    <CheckBox IsChecked="{Binding Value}"
                              HorizontalAlignment="Left"
                              VerticalAlignment="Center" />
                </DataTemplate>
                <DataTemplate x:Key="IntegerTemplate"
                              DataType="{x:Type local:BlackboardKeyViewModel}">
                    <TextBox Text="{Binding Value}" />
                </DataTemplate>
                <DataTemplate x:Key="DecimalTemplate"
                              DataType="{x:Type local:BlackboardKeyViewModel}">
                    <TextBox Text="{Binding Value}" />
                </DataTemplate>
                <DataTemplate x:Key="StringTemplate"
                              DataType="{x:Type local:BlackboardKeyViewModel}">
                    <TextBox Text="{Binding Value}" />
                </DataTemplate>
                <DataTemplate x:Key="ObjectTemplate"
                              DataType="{x:Type local:BlackboardKeyViewModel}">
                    <TextBox Text="{Binding Value}"
                             IsEnabled="False" />
                </DataTemplate>
                <DataTemplate x:Key="KeyTemplate"
                              DataType="{x:Type local:BlackboardKeyViewModel}">
                    <ComboBox ItemsSource="{Binding DataContext.Blackboard.Keys, Source={StaticResource EditorProxy}}"
                              SelectedItem="{Binding Value, Mode=OneWayToSource}"
                              DisplayMemberPath="Name" />
                </DataTemplate>
            </Expander.Resources>
            <Border BorderBrush="#333336"
                    BorderThickness="1"
                    Width="250"
                    Padding="10"
                    HorizontalAlignment="Stretch">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition MinHeight="200" />
                        <RowDefinition MinHeight="200" />
                    </Grid.RowDefinitions>

                    <!--TRANSITIONS-->
                    <Grid>
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <ItemsControl ItemsSource="{Binding Transitions}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate DataType="{x:Type local:TransitionViewModel}">
                                        <Expander BorderThickness="0 0 0 1"
                                                  BorderBrush="#333337">
                                            <Expander.Style>
                                                <Style TargetType="{x:Type Expander}"
                                                       BasedOn="{StaticResource {x:Type Expander}}">
                                                    <Setter Property="Tag"
                                                            Value="{StaticResource ExpandRightIcon}" />
                                                    <Style.Triggers>
                                                        <Trigger Property="IsExpanded"
                                                                 Value="True">
                                                            <Setter Property="Tag"
                                                                    Value="{StaticResource ExpandDownIcon}" />
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Expander.Style>
                                            <Expander.Header>
                                                <TextBlock>
                                                    <TextBlock.Style>
                                                        <Style TargetType="{x:Type TextBlock}">
                                                            <Setter Property="Foreground"
                                                                    Value="White" />
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsActive}"
                                                                             Value="True">
                                                                    <Setter Property="Foreground"
                                                                            Value="#8DD28A" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                    <Run Text="{Binding Source.Name, Mode=OneWay}" />
                                                    <Run Text="🠚" />
                                                    <Run Text="{Binding Target.Name, Mode=OneWay}" />
                                                </TextBlock>
                                            </Expander.Header>
                                            <Border BorderBrush="#333336"
                                                    BorderThickness="1"
                                                    HorizontalAlignment="Stretch">
                                                <!--TODO: Condition Editor-->
                                            </Border>
                                        </Expander>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>

                    <!--NODES-->
                    <Grid Visibility="{Binding SelectedStates.Count, Converter={local:BooleanToVisibilityConverter}}"
                          Grid.Row="1">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition />
                        </Grid.RowDefinitions>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <local:EditableTextBlock Text="{Binding SelectedState.Name}"
                                                     IsEditing="{Binding IsChecked, ElementName=EditStateName}"
                                                     IsEditable="{Binding SelectedState.IsEditable}"
                                                     FontWeight="Bold"
                                                     FontSize="16"
                                                     MaxLength="20" />
                            <CheckBox x:Name="EditStateName"
                                      Visibility="{Binding SelectedState.IsEditable, Converter={local:BooleanToVisibilityConverter}}"
                                      Content="{StaticResource EditIcon}"
                                      Style="{StaticResource IconCheckBox}"
                                      Grid.Column="1" />
                        </Grid>

                        <Separator Height="2"
                                   Background="#333337"
                                   Width="Auto"
                                   Margin="0 2 0 10"
                                   Grid.Row="1" />

                        <Grid Grid.Row="2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition />
                            </Grid.RowDefinitions>

                            <TextBlock Text="Action" />
                            <ComboBox ItemsSource="{Binding Blackboard.Actions}"
                                      SelectedItem="{Binding SelectedState.Action}"
                                      SelectedIndex="0"
                                      DisplayMemberPath="Name"
                                      Grid.Column="1" />

                            <!--INPUT-->
                            <Expander Margin="0 5 0 0"
                                      Grid.Row="1"
                                      Grid.ColumnSpan="2"
                                      BorderThickness="0 0 0 1"
                                      BorderBrush="#333337"
                                      Visibility="{Binding SelectedState.Action.Input.Count, Converter={local:BooleanToVisibilityConverter}}">

                                <Expander.Style>
                                    <Style TargetType="{x:Type Expander}"
                                           BasedOn="{StaticResource {x:Type Expander}}">
                                        <Setter Property="Tag"
                                                Value="{StaticResource ExpandRightIcon}" />
                                        <Style.Triggers>
                                            <Trigger Property="IsExpanded"
                                                     Value="True">
                                                <Setter Property="Tag"
                                                        Value="{StaticResource ExpandDownIcon}" />
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Expander.Style>
                                <Expander.Header>
                                    <TextBlock Text="Input"
                                               FontWeight="Bold" />
                                </Expander.Header>
                                <StackPanel>
                                    <ItemsControl ItemsSource="{Binding SelectedState.Action.Input}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Grid IsSharedSizeScope="True">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition />
                                                        <ColumnDefinition />
                                                        <ColumnDefinition />
                                                    </Grid.ColumnDefinitions>
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition />
                                                        <RowDefinition />
                                                    </Grid.RowDefinitions>

                                                    <TextBlock Text="{Binding Name}" />

                                                    <TextBox Text="{Binding Type}"
                                                             IsEnabled="False"
                                                             Margin="5 0"
                                                             Grid.Column="1" />

                                                    <ContentPresenter Content="{Binding}"
                                                                      x:Name="ValueEditor"
                                                                      Grid.Column="2" />
                                                </Grid>
                                                <DataTemplate.Triggers>
                                                    <DataTrigger Binding="{Binding Type}"
                                                                 Value="Boolean">
                                                        <Setter Property="ContentTemplate"
                                                                Value="{StaticResource BooleanTemplate}"
                                                                TargetName="ValueEditor" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Type}"
                                                                 Value="Integer">
                                                        <Setter Property="ContentTemplate"
                                                                Value="{StaticResource IntegerTemplate}"
                                                                TargetName="ValueEditor" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Type}"
                                                                 Value="Double">
                                                        <Setter Property="ContentTemplate"
                                                                Value="{StaticResource DecimalTemplate}"
                                                                TargetName="ValueEditor" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Type}"
                                                                 Value="String">
                                                        <Setter Property="ContentTemplate"
                                                                Value="{StaticResource StringTemplate}"
                                                                TargetName="ValueEditor" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Type}"
                                                                 Value="Object">
                                                        <Setter Property="ContentTemplate"
                                                                Value="{StaticResource ObjectTemplate}"
                                                                TargetName="ValueEditor" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Type}"
                                                                 Value="Key">
                                                        <Setter Property="ContentTemplate"
                                                                Value="{StaticResource KeyTemplate}"
                                                                TargetName="ValueEditor" />
                                                    </DataTrigger>
                                                </DataTemplate.Triggers>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Expander>

                            <!--OUTPUT-->
                            <Expander Margin="0 5 0 0"
                                      Grid.Row="2"
                                      Grid.ColumnSpan="2"
                                      BorderThickness="0 0 0 1"
                                      BorderBrush="#333337"
                                      Visibility="{Binding SelectedState.Action.Output.Count, Converter={local:BooleanToVisibilityConverter}}">
                                <Expander.Style>
                                    <Style TargetType="{x:Type Expander}"
                                           BasedOn="{StaticResource {x:Type Expander}}">
                                        <Setter Property="Tag"
                                                Value="{StaticResource ExpandRightIcon}" />
                                        <Style.Triggers>
                                            <Trigger Property="IsExpanded"
                                                     Value="True">
                                                <Setter Property="Tag"
                                                        Value="{StaticResource ExpandDownIcon}" />
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Expander.Style>
                                <Expander.Header>
                                    <TextBlock Text="Output"
                                               FontWeight="Bold" />
                                </Expander.Header>
                                <StackPanel>
                                    <Separator Height="2"
                                               Background="#333337"
                                               Width="Auto" />

                                    <ItemsControl ItemsSource="{Binding SelectedState.Action.Output}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Grid IsSharedSizeScope="True">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition />
                                                        <ColumnDefinition />
                                                        <ColumnDefinition />
                                                    </Grid.ColumnDefinitions>
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition />
                                                        <RowDefinition />
                                                    </Grid.RowDefinitions>

                                                    <TextBlock Text="{Binding Name}" />

                                                    <TextBox Text="{Binding Type}"
                                                             IsEnabled="False"
                                                             Margin="5 0"
                                                             Grid.Column="1" />

                                                    <ContentPresenter Content="{Binding}"
                                                                      x:Name="ValueEditor"
                                                                      Grid.Column="2" />
                                                </Grid>
                                                <DataTemplate.Triggers>
                                                    <DataTrigger Binding="{Binding Type}"
                                                                 Value="Boolean">
                                                        <Setter Property="ContentTemplate"
                                                                Value="{StaticResource BooleanTemplate}"
                                                                TargetName="ValueEditor" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Type}"
                                                                 Value="Integer">
                                                        <Setter Property="ContentTemplate"
                                                                Value="{StaticResource IntegerTemplate}"
                                                                TargetName="ValueEditor" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Type}"
                                                                 Value="Double">
                                                        <Setter Property="ContentTemplate"
                                                                Value="{StaticResource DecimalTemplate}"
                                                                TargetName="ValueEditor" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Type}"
                                                                 Value="String">
                                                        <Setter Property="ContentTemplate"
                                                                Value="{StaticResource StringTemplate}"
                                                                TargetName="ValueEditor" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Type}"
                                                                 Value="Object">
                                                        <Setter Property="ContentTemplate"
                                                                Value="{StaticResource ObjectTemplate}"
                                                                TargetName="ValueEditor" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Type}"
                                                                 Value="Key">
                                                        <Setter Property="ContentTemplate"
                                                                Value="{StaticResource KeyTemplate}"
                                                                TargetName="ValueEditor" />
                                                    </DataTrigger>
                                                </DataTemplate.Triggers>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Expander>
                        </Grid>
                    </Grid>

                    <!--BLACKBOARD-->
                    <Grid Visibility="{Binding SelectedStates.Count, Converter={local:BooleanToVisibilityConverter Negate=True}}"
                          Grid.Row="1">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition />
                        </Grid.RowDefinitions>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <local:EditableTextBlock Text="{Binding Name}"
                                                     IsEditing="{Binding IsChecked, ElementName=EditName}"
                                                     FontWeight="Bold"
                                                     FontSize="16"
                                                     MaxLength="20" />

                            <StackPanel Orientation="Horizontal"
                                        Grid.Column="1">
                                <CheckBox x:Name="EditName"
                                          Content="{StaticResource EditIcon}"
                                          ToolTip="Edit Name"
                                          Style="{StaticResource IconCheckBox}" />
                                <Button Content="{StaticResource AddKeyIcon}"
                                        Command="{Binding Blackboard.AddKeyCommand}"
                                        ToolTip="Add New Key"
                                        Style="{StaticResource IconButton}" />
                            </StackPanel>
                        </Grid>

                        <Separator Height="2"
                                   Background="#333337"
                                   Width="Auto"
                                   Margin="0 2 0 10"
                                   Grid.Row="1" />

                        <ScrollViewer VerticalScrollBarVisibility="Auto"
                                      Grid.Row="2">
                            <ItemsControl ItemsSource="{Binding Blackboard.Keys}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate DataType="{x:Type local:BlackboardKeyViewModel}">
                                        <Grid IsSharedSizeScope="True">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition />
                                                <ColumnDefinition />
                                                <ColumnDefinition />
                                            </Grid.ColumnDefinitions>
                                            <Grid.RowDefinitions>
                                                <RowDefinition />
                                                <RowDefinition />
                                            </Grid.RowDefinitions>

                                            <local:EditableTextBlock Text="{Binding Name}" />

                                            <ComboBox ItemsSource="{Binding Type, Converter={local:EnumValuesConverter}}"
                                                      DisplayMemberPath="Name"
                                                      SelectedValuePath="Value"
                                                      SelectedValue="{Binding Type}"
                                                      Margin="5 0"
                                                      Grid.Column="1" />

                                            <ContentPresenter Content="{Binding}"
                                                              x:Name="ValueEditor"
                                                              Grid.Column="2" />

                                            <Separator Height="2"
                                                       Background="#333337"
                                                       Width="Auto"
                                                       Margin="0 2 0 10"
                                                       Grid.Row="1"
                                                       Grid.ColumnSpan="3" />
                                        </Grid>
                                        <DataTemplate.Triggers>
                                            <DataTrigger Binding="{Binding Type}"
                                                         Value="Boolean">
                                                <Setter Property="ContentTemplate"
                                                        Value="{StaticResource BooleanTemplate}"
                                                        TargetName="ValueEditor" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Type}"
                                                         Value="Integer">
                                                <Setter Property="ContentTemplate"
                                                        Value="{StaticResource IntegerTemplate}"
                                                        TargetName="ValueEditor" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Type}"
                                                         Value="Double">
                                                <Setter Property="ContentTemplate"
                                                        Value="{StaticResource DecimalTemplate}"
                                                        TargetName="ValueEditor" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Type}"
                                                         Value="String">
                                                <Setter Property="ContentTemplate"
                                                        Value="{StaticResource StringTemplate}"
                                                        TargetName="ValueEditor" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Type}"
                                                         Value="Object">
                                                <Setter Property="ContentTemplate"
                                                        Value="{StaticResource ObjectTemplate}"
                                                        TargetName="ValueEditor" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Type}"
                                                         Value="Key">
                                                <Setter Property="ContentTemplate"
                                                        Value="{StaticResource KeyTemplate}"
                                                        TargetName="ValueEditor" />
                                            </DataTrigger>
                                        </DataTemplate.Triggers>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </Grid>
            </Border>
        </Expander>

        <!--TOOLBAR-->
        <Border CornerRadius="2"
                Background="#2D2D30"
                BorderThickness="0 0 0 1"
                HorizontalAlignment="Center"
                VerticalAlignment="Top"
                Margin="10 0">
            <StackPanel Orientation="Horizontal">
                <Button Command="{Binding PauseCommand}">
                    <Button.Style>
                        <Style TargetType="{x:Type Button}"
                               BasedOn="{StaticResource IconButton}">
                            <Setter Property="Content"
                                    Value="{StaticResource PauseIcon}" />
                            <Setter Property="ToolTip"
                                    Value="Pause" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Runner.State}"
                                             Value="Stopped">
                                    <Setter Property="Visibility"
                                            Value="Collapsed" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Runner.State}"
                                             Value="Paused">
                                    <Setter Property="Content"
                                            Value="{StaticResource UnpauseIcon}" />
                                    <Setter Property="ToolTip"
                                            Value="Continue" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                <Button Command="{Binding RunCommand}"
                        Style="{StaticResource IconButton}">
                    <StackPanel Orientation="Horizontal">
                        <ContentPresenter DataContext="{Binding}"
                                          Margin="0 0 4 0">
                            <ContentPresenter.Style>
                                <Style TargetType="{x:Type ContentPresenter}">
                                    <Setter Property="Content"
                                            Value="{StaticResource StopIcon}" />
                                    <Setter Property="ToolTip"
                                            Value="Stop" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Runner.State}"
                                                     Value="Stopped">
                                            <Setter Property="Content"
                                                    Value="{StaticResource RunIcon}" />
                                            <Setter Property="ToolTip"
                                                    Value="Run" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ContentPresenter.Style>
                        </ContentPresenter>
                        <TextBlock Text="{Binding Name}" />
                    </StackPanel>
                </Button>
                <Separator Height="Auto"
                           BorderThickness="0 0 1 0" />
                <Button Content="{StaticResource ZoomInIcon}"
                        ToolTip="Zoom In"
                        Style="{StaticResource IconButton}" />
                <Button Content="{StaticResource ZoomOutIcon}"
                        ToolTip="Zoom Out"
                        Style="{StaticResource IconButton}" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
