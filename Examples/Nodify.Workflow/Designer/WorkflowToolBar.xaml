<UserControl x:Class="Nodify.Workflow.Designer.WorkflowToolBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ic="clr-namespace:FluentIcons.Wpf;assembly=FluentIcons.Wpf"
             xmlns:local="clr-namespace:Nodify.Workflow.Designer"
             d:DataContext="{d:DesignInstance local:SubWorkflowDesignerViewModel, IsDesignTimeCreatable=True}"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">

    <Border Padding="8"
            CornerRadius="{DynamicResource OverlayCornerRadius}"
            Background="{DynamicResource LayerOnAcrylicFillColorDefaultBrush}"
            BorderBrush="{DynamicResource AccentControlElevationBorderBrush}"
            BorderThickness="1">
        <StackPanel Orientation="Horizontal">
            <ContentControl Content="{Binding ZoomInCommand}"
                            Focusable="False" />
            <TextBlock Text="{Binding ZoomLevel.Value, StringFormat={}{0:P0}}"
                       Margin="4 0 4 4"
                       VerticalAlignment="Center" />
            <ContentControl Content="{Binding ZoomOutCommand}"
                            Focusable="False" />

            <Border Style="{StaticResource SeparatorStyle.Default}" />

            <ContentControl Content="{Binding FitToViewCommand}"
                            Focusable="False" />
            <ContentControl Content="{Binding LockViewCommand}"
                            Focusable="False" />

            <Border Style="{StaticResource SeparatorStyle.Default}" />

            <Button Style="{StaticResource ButtonStyle.Borderless}"
                    ToolTip="More actions">
                <ic:FluentIcon IsHitTestVisible="False"
                               Icon="MoreHorizontal"
                               IconSize="Size16"
                               FontSize="14"
                               VerticalAlignment="Center">
                </ic:FluentIcon>
            </Button>
        </StackPanel>
    </Border>
</UserControl>
