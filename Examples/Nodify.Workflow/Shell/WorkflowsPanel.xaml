<UserControl x:Class="Nodify.Workflow.Shell.WorkflowsPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Nodify.Workflow.Shell"
             xmlns:wf="clr-namespace:Nodify.Workflow.Designer"
             xmlns:ic="clr-namespace:FluentIcons.Wpf;assembly=FluentIcons.Wpf"
             d:DataContext="{d:DesignInstance local:ApplicationViewModel, IsDesignTimeCreatable=True}"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">

    <UserControl.LayoutTransform>
        <ScaleTransform ScaleX="0" />
    </UserControl.LayoutTransform>
    <UserControl.Style>
        <Style TargetType="UserControl">
            <Setter Property="Opacity"
                    Value="0" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsWorkflowsPanelOpen.Value}"
                             Value="True">
                    <DataTrigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="LayoutTransform.(ScaleTransform.ScaleX)"
                                                 To="1"
                                                 Duration="0:0:0.3">
                                    <DoubleAnimation.EasingFunction>
                                        <CubicEase EasingMode="EaseOut" />
                                    </DoubleAnimation.EasingFunction>
                                </DoubleAnimation>
                                <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                 To="1"
                                                 Duration="0:0:0.2" />
                            </Storyboard>
                        </BeginStoryboard>
                    </DataTrigger.EnterActions>
                    <DataTrigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="LayoutTransform.(ScaleTransform.ScaleX)"
                                                 To="0"
                                                 Duration="0:0:0.25">
                                    <DoubleAnimation.EasingFunction>
                                        <CubicEase EasingMode="EaseIn" />
                                    </DoubleAnimation.EasingFunction>
                                </DoubleAnimation>
                                <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                 To="0"
                                                 Duration="0:0:0.15" />
                            </Storyboard>
                        </BeginStoryboard>
                    </DataTrigger.ExitActions>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Style>

    <Border Grid.Row="1"
            HorizontalAlignment="Left"
            VerticalAlignment="Stretch"
            d:Background="White"
            Width="200">
        <Border.Child>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <ListView ItemsSource="{Binding Workflows}"
                          HorizontalAlignment="Stretch"
                          SelectedValue="{Binding SelectedWorkflow.Value, Mode=TwoWay}"
                          SelectionMode="Single">
                    <ListView.ItemTemplate>
                        <DataTemplate DataType="{x:Type wf:WorkflowDesignerViewModel}">
                            <TextBlock Margin="8,4"
                                       Text="{Binding Name.Value, Mode=OneWay}" />
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <Button Grid.Row="1" HorizontalAlignment="Stretch">
                    <StackPanel Orientation="Horizontal">
                        <ic:FluentIcon Icon="Add" />
                        <TextBlock Text="New Workflow"
                                   Margin="4 0 0 0" />
                    </StackPanel>
                </Button>
            </Grid>
        </Border.Child>
    </Border>

</UserControl>
