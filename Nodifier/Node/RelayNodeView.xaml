<UserControl x:Class="Nodifier.Views.RelayNodeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Nodifier.Views"
             xmlns:vm="clr-namespace:Nodifier"
             xmlns:nodify="https://miroiu.github.io/nodify"
             xmlns:s="https://github.com/canton7/Stylet"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=vm:IRelayNode, IsDesignTimeCreatable=False}">
    <nodify:KnotNode Content="{Binding Connector}">
        <nodify:KnotNode.ContentTemplate>
            <DataTemplate>
                <ContentControl s:View.Model="{Binding}" />
            </DataTemplate>
        </nodify:KnotNode.ContentTemplate>
    </nodify:KnotNode>
</UserControl>
