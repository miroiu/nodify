<UserControl x:Class="Nodifier.NodeGraph"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:s="clr-namespace:Nodifier.XAML"
             xmlns:vm="clr-namespace:Nodifier"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=vm:IGraphWidget, IsDesignTimeCreatable=False}">
    <UserControl.InputBindings>
        <KeyBinding Key="Delete" Command="{s:Action DeleteSelection}" s:View.ActionTarget="{Binding}" />
        <KeyBinding Key="Z" Modifiers="Ctrl" Command="{s:Action Undo}" s:View.ActionTarget="{Binding History}" />
        <KeyBinding Key="Z" Modifiers="Ctrl+Shift" Command="{s:Action Redo}" s:View.ActionTarget="{Binding History}" />
        <KeyBinding Key="Y" Modifiers="Ctrl" Command="{s:Action Redo}" s:View.ActionTarget="{Binding History}" />
    </UserControl.InputBindings>
    <ContentControl s:View.Model="{Binding}" />
</UserControl>
