<Window x:Class="Nodify.MinimalExample.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NodifyMinimalExample" 
        xmlns:blueprint="https://miroiu.github.io/nodify-blueprint" 
        xmlns:s="https://github.com/canton7/Stylet"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <local:MinimalApp />
    </Window.DataContext>

    <Grid>
        <blueprint:NodeGraph DataContext="{Binding Graph}" />
        <Button Command="{s:Action AddComment}"
                s:View.ActionTarget="{Binding}"
                HorizontalAlignment="Center"
                VerticalAlignment="Top">Add comment</Button>
        <Button Command="{s:Action FocusRandomNode}"
                s:View.ActionTarget="{Binding}"
                HorizontalAlignment="Center"
                VerticalAlignment="Top">Focus random node</Button>
    </Grid>
</Window>
