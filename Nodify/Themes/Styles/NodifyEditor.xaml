<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:Nodify">

    <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />

    <DataTemplate x:Key="DefaultConnectionTemplate">
        <local:Connection />
    </DataTemplate>

    <Style x:Key="ConnectionsItemsControl"
           TargetType="ItemsControl">
        <Setter Property="ItemTemplate"
                Value="{StaticResource DefaultConnectionTemplate}" />
        <Setter Property="ItemsPanel">
            <Setter.Value>
                <ItemsPanelTemplate>
                    <Canvas />
                </ItemsPanelTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="SelectionRectangleStyle"
           TargetType="Rectangle">
        <Setter Property="Stroke"
                Value="DodgerBlue" />
        <Setter Property="StrokeThickness"
                Value="1" />
        <Setter Property="Fill">
            <Setter.Value>
                <SolidColorBrush Opacity="0.1"
                                 Color="DodgerBlue" />
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type local:NodifyEditor}">
        <Setter Property="ClipToBounds"
                Value="True" />
        <Setter Property="Foreground"
                Value="White" />
        <Setter Property="Background"
                Value="#1E1E1E" />
        <Setter Property="IsTabStop"
                Value="False" />
        <Setter Property="EnableRealtimeSelection"
                Value="True" />
        <Setter Property="SelectionRectangleStyle"
                Value="{StaticResource SelectionRectangleStyle}" />
        <Setter Property="ConnectionTemplate"
                Value="{StaticResource DefaultConnectionTemplate}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:NodifyEditor}">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <Canvas RenderTransform="{TemplateBinding AppliedTransform}">
                            <ItemsControl x:Name="PART_ConnectionsHost"
                                          Style="{StaticResource ConnectionsItemsControl}"
                                          ItemsSource="{TemplateBinding Connections}"
                                          ItemTemplate="{TemplateBinding ConnectionTemplate}"
                                          IsTabStop="False" />

                            <local:NodifyCanvas x:Name="PART_ItemsHost"
                                                IsItemsHost="True" />

                            <Rectangle x:Name="PART_SelectionRectangle"
                                       Style="{TemplateBinding SelectionRectangleStyle}"
                                       Canvas.Top="{Binding SelectingRectangle.Y, RelativeSource={RelativeSource TemplatedParent}}"
                                       Canvas.Left="{Binding SelectingRectangle.X, RelativeSource={RelativeSource TemplatedParent}}"
                                       Width="{Binding SelectingRectangle.Width, RelativeSource={RelativeSource TemplatedParent}}"
                                       Height="{Binding SelectingRectangle.Height, RelativeSource={RelativeSource TemplatedParent}}"
                                       Visibility="{TemplateBinding IsSelecting, Converter={StaticResource BooleanToVisibilityConverter}}" />

                            <local:PendingConnection IsTabStop="False" />
                        </Canvas>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>