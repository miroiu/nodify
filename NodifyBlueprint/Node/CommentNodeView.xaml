<UserControl x:Class="NodifyBlueprint.Views.CommentNodeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:nodify="https://miroiu.github.io/nodify"
             xmlns:vm="clr-namespace:NodifyBlueprint"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=vm:ICommentNode, IsDesignTimeCreatable=False}">
    <nodify:GroupingNode 
        ActualSize="{Binding CommentSize, Mode=TwoWay}"
        CanResize="{Binding CanResize}"
        Header="{Binding Title}" />
</UserControl>
